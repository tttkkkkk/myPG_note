
<%= link_to "ADD", new_note_path , class: 'btn btn-success'%>
<div class="container">
 <table class="table table-striped">
   <thead>
   </thead>
   <tbody>
   <% if !@notes.nil? %>
   <% @notes.each do |f| %>
      <%= simple_format f.content %>
   <% end %>
   <% end %>
   </tbody>
 </table>
</div>
